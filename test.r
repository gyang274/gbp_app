require(shiny)

# setwd("f:/technism/r/r_pkgs/bpp/app/")

# runApp(appDir = getwd(), port = 8572)

runApp(appDir = getwd(), port = 8572, launch.browser = TRUE)

# runApp(appDir = getwd(), port = 8572, display.mode = "showcase")
